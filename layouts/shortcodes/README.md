# Old Shortcodes Directory

This directory uses the old Hugo naming convention (`shortcodes/` without underscore).

In Hugo v0.146.0+, shortcodes should be in `layouts/_shortcodes/` (with underscore).

**All shortcodes have been migrated to:** `layouts/_shortcodes/`

This directory can be safely removed after verifying everything works.
